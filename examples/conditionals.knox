func main() void {
    if true || false {

    } else if false {

    } else {

    }
    while false {
        var foo : int = bar(1,2);
    }
    for item : int in stl.range(1, 10, 1) {
        stl.print(item);
    }
}

func bar(a : int, b : int) int {
    return a + b;
}

